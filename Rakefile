require 'rubygems'
require 'rake'

begin
  require 'jeweler'
  Jeweler::Tasks.new do |gem|
    gem.name = "grizzly-http"

    gem.summary = %Q{A JavaGem version of grizzly-http - packaged by JavaGems (http://www.javagems.org) - original by SUN Microsystems (http://www.sun.com)}



    gem.description = %Q{grizzly-http packaged up in a JavaGem}

    gem.homepage = "http://www.javagems.org/"

    gem.authors = ["pending/unknown"]

    gem.email = "autobuild@javagems.org"
    gem.version = "1.9.10"

  

    gem.add_development_dependency "grizzly-portunif", "~>1.9.10"

    gem.add_development_dependency "grizzly-rcm", "~>1.9.10"

    gem.add_development_dependency "grizzly-framework", "~>1.9.10"

  end
rescue LoadError
  puts "Jeweler (or a dependency) not available. Install it with: sudo gem install jeweler"
end

task :default => :build
